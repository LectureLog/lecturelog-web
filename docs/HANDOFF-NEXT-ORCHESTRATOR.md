# Промпт/брифинг для СЛЕДУЮЩЕГО оркестрирующего вызова LectureLog

> ⚡ КАК ЗАПУСТИТЬ В ЧИСТОМ ЧАТЕ: открой новый чат в этом проекте и отправь ОДНУ строку:
> **«Ты оркестратор LectureLog. Прочитай docs/HANDOFF-NEXT-ORCHESTRATOR.md и продолжай.»**
> Всё остальное (состояние, задачи, правила, Codex, грабли) оркестратор поднимет сам по
> чек-листу ниже. Память проекта (MEMORY.md) подгружается автоматически и продублирует
> ключевые правила (Codex для кодинга, оркестратор-не-кодит).
>
> ⛳ ГДЕ МЫ СЕЙЧАС (2026-06-26, 3): ВСЕ кодовые атомы волны C1 завершены, смержены в `integration`
> и ЗАПУШЕНЫ в origin (HEAD `f49b633`). Добавлен внеплановый атом **dev-deploy** (два режима запуска
> web: dev hot-reload + prod «одной кнопкой»). Ворота зелёные, дерево чисто, worktree-ов нет.
> СЛЕДУЮЩИЙ ШАГ: владелец поднимает РЕАЛЬНОЕ ядро в Docker → прогнать **GATE C1 e2e** (сквозной:
> вход→загрузка→ядро→вебхук ready→/hub→читалка `/read/{id}`). Реальный e2e reader был заблокирован
> отсутствием `structure.json` в ядре — теперь разблокируется. PR в `main` владелец пока НЕ открывает
> (выбрал только push). См. раздел СОСТОЯНИЕ ниже и `docs/HANDOFF-C1.md`.

---

## ЧЕК-ЛИСТ СТАРТА (выполни ПО ПОРЯДКУ, прежде чем что-либо делать)
1. Прочитай ЭТОТ файл целиком (роль, политика, грабли, правила Codex/паузы).
2. Прочитай `docs/HANDOFF-C1.md` — ТЕКУЩАЯ волна: состояние, порядок атомов, долги,
   РАСПРЕДЕЛЕНИЕ РОЛЕЙ Codex/Claude. ← главный файл «что делать сейчас».
3. Прочитай `docs/WORKFLOW.md` (процесс волн) и `docs/TASKS.md` §C1 (содержание атомов).
4. При планировании атома сверяйся с `docs/plans/2026-06-22-platform-design.md` (нужные §§).
5. `docs/HANDOFF-C0.md` — историческая справка по фундаменту (читать при необходимости).
6. Проверь Codex: скилл `/codex:setup` (ждём ready+authenticated). Кодинг/ревью — на Codex
   (его тариф, не жрёт бюджет Claude). Если не ready — временно Sonnet.
7. Бюджет: `cswap --status`. Правило паузы — при 5h=95% закругляйся (раздел ниже).
8. Git: ветка `integration`, дерево чисто, ворота зелёные
   (`go build ./... && go vet ./... && go test ./...`) — перепроверь САМ.

## ПРОМПТ (роль — держать всю сессию)

Ты — ОРКЕСТРАТОР разработки LectureLog по `docs/WORKFLOW.md`. Код руками НЕ пишешь:
планируешь, запускаешь сабагентов (Codex-кодер / Codex-ревьюер / Plan-агент /
Claude-приёмщик / docs), читаешь вердикты, ведёшь ветки/worktree, мержишь, САМ гоняешь
ворота. Не сваливайся в исполнителя/архитектора — даже мелкий фикс из ревью через сабагента.

СОСТОЯНИЕ: волна C0 ЗАВЕРШЕНА+ОПУБЛИКОВАНА (origin 2ef594f..0507824). Все кодовые атомы C1 завершены
и смержены в локальную `integration`, включая **C1-reader** (бэкенд A/B1/B2, UI C и проводка D), плюс
внеплановый атом **dev-deploy** (два режима запуска web). HEAD `integration` = `f49b633`, ЗАПУШЕН в
origin. ОСТАЛОСЬ: GATE C1 e2e (сквозной reader был заблокирован ядром — нет `structure.json`; владелец
поднимает ядро, e2e разблокируется). PR в `main` пока НЕ открыт (владелец 2026-06-26 выбрал только push).
NB окружение: в этой сессии Go/Node БЫЛИ на месте (не пропадали). Codex: запускать `codex exec ... </dev/null`.
**НОВАЯ СХЕМА (владелец 2026-06-26):** Codex билдит+КОММИТИТ сам → отдельный агент проверяет → оркестратор
решает мердж. НО песочница Codex не даёт коммитить (.git read-only) — нужно правило `Bash(codex exec:*)` в
правах (добавляет ВЛАДЕЛЕЦ) + флаг `--dangerously-bypass-approvals-and-sandbox`. Пока правила нет — Codex
билдит (sandbox ON), оркестратор коммитит механически. Детали — HANDOFF-C1 раздел «НОВАЯ СХЕМА РОЛЕЙ».

РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-06-24): PR сейчас НЕ создаём — накапливаем в integration до конца
волны C1, PR в main позже. (Уточняет раннюю формулировку «push + PR в конце C0».)

ВОЛНА C1: кодовые атомы завершены. Прочитай `docs/HANDOFF-C1.md` — детальное состояние,
грабли Codex/окружения.
- C1-lecture ✅ (48f55b3), C1-upload ✅ 7/7 (merge df35653), C1-sync ✅ (merge 2a96b75),
  C1-devstack ✅ (merge ddc0b6d), C1-hub ✅ (merge 0ce083f).
- C1-reader ✅: бэкенд A/B1/B2 (merge `3b306c4`), UI C (merge `f2111fa`) и проводка D
  (merge `49e2b29`). `/hub` ведёт на рабочий `/read/{id}`; чтение public-лекции доступно анониму,
  private — владельцу. Экспорт `/read/{id}/export` перенаправляет на presigned-архив. Попутно
  устранён дрейф `app.css` (fix `dd2fc11`).
- **dev-deploy ✅** (merge `f49b633`, внеплановый по просьбе владельца): ДВА режима запуска web.
  DEV (быстрые правки): `make up-stub` (Postgres + MinIO-заглушка в профиле `stub-minio`) → `make watch`
  (air hot-reload) или `make dev` (go run). PROD («одной кнопкой»): `make prod` = `Dockerfile`
  (multi-stage golang→alpine, CGO off, 53МБ, образ собран и проверен) + `docker-compose.prod.yml`
  (web+postgres, `host.docker.internal` к ядру). `.env.example` имеет пресет для e2e с реальным ядром.
- ОСТАЛОСЬ: GATE C1 e2e (core-blocked был `structure.json`; ядро поднимается владельцем). PR в `main`
  отложен (владелец выбрал только push). origin синхронен по `integration` (HEAD `f49b633`).
NB: ориентир лимита — 5h-окно (НЕ 7d), работать до 5h=95%; Codex = gpt-5.5/medium, сеть в его
песочнице включена (см. HANDOFF-C1 раздел CODEX).

ПОЛИТИКА ВЛАДЕЛЬЦА (НЕ переоткрывать):
- Мержи готовые атомы в ЛОКАЛЬНУЮ integration СРАЗУ (--no-ff, без PR на каждый атом).
  Push + PR — в КОНЦЕ волны. (Это сменило раннюю политику «PR на каждый атом» из B1.)
- Веди автономно; зови владельца в конце волны ИЛИ останавливайся ЗАРАНЕЕ при лимитах.
- ЭКОНОМИЯ ТОКЕНОВ — КОДИНГ И РЕВЬЮ ОТДАЁМ CODEX (свой тариф OpenAI, НЕ жрёт лимиты Claude;
  проверено: codex-cli, authenticated, плагин `codex:`, вызов через Agent subagent_type
  `codex:codex-rescue` или скилл `codex:rescue`). Распределение — см. HANDOFF-C1.md раздел
  «РАСПРЕДЕЛЕНИЕ РОЛЕЙ»: BUILD+REVIEW(отдельный инстанс!)+LOOP+диагностика → Codex;
  PLAN(Plan-агент)+ACCEPT(Sonnet, остаётся на Claude — 2 независимых движка)+DOCS(Sonnet) → Claude.
- Мелкие механические правки (gofmt, go mod tidy, git-операции) оркестратор делает САМ,
  не плодя сабагента. ОСМЫСЛЕННЫЕ правки кода — ВСЕГДА через исполнителя (LOOP), даже мелкий
  фикс из ревью «всего 4 строки» (владелец отчитал 2026-06-24 за коддинг руками; см. память
  feedback-orchestrator-no-coding). Граница: тест/логика/функция = сабагент; gofmt/git = сам.
- ДРОБИТЬ атомы: исполнитель не должен сидеть часами/гнить контекст. Объёмный атом → под-атомы
  с отдельными исполнителями. Где задачи независимы — распараллеливать (владелец просил).

ЛИМИТЫ: команда `cswap --status` (shell) показывает 5h/7d окна тарифа. ПРАВИЛО ПАУЗЫ
(уточнено владельцем 2026-06-24): когда **5h-окно = 95%** — закругляй на безопасной границе
(после мержа атома/до старта нового), допиши handoff (HANDOFF-C1 + этот файл), продолжишь в
чистом чате. Прайм-кэш 5 мин — длинные паузы дороже. `/usage` как shell НЕ работает (CLI).

---

## КАК ВЕДЁТСЯ АТОМ (8 шагов, обкатано на 4 атомах C0)
PLAN(Plan-агент→PLAN.md в worktree, в git exclude, НЕ коммитить) → ISOLATE(git worktree
.worktrees/<задача>, ветка node/<задача> от integration) → BUILD(исполнитель Sonnet, TDD) →
ACCEPT(приёмщик Sonnet, ≠исполнитель, 2 слоя: ворота + смысл; вердикт COMPLETE|INCOMPLETE) →
REVIEW(superpowers:code-reviewer) → LOOP(замечания→исполнителю, лимит 3 круга) →
MERGE(--no-ff в локальную integration) → DOCS(отдельный docs-субагент, правило владельца).

ОРКЕСТРАТОР ВСЕГДА перепроверяет ворота САМ фактически (не верит отчёту сабагента) перед ACCEPT
и перед мержем: `make gate` (для web/auth) или `go build/vet/test`; проверяет ЧИСТОТУ ДЕРЕВА после
gate, gofmt, детерминизм генерации, что артефакты/бинари не закоммичены.

---

## «ГРАБЛИ», на которые мы УЖЕ наступили (проверяй каждый раз!)

1. **LSP основного дерева даёт ЛОЖНЫЕ ошибки про worktree-код.** Файлы атома живут только в
   `.worktrees/<задача>`; LSP/диагностика основного дерева показывает «undefined: X»,
   «missing go.sum entry», «not used», «This file is within module .worktrees/...». ЭТО АРТЕФАКТ —
   игнорируй. Истина = фактическая сборка В worktree (`cd .worktrees/<задача> && go build ./...`).
   Каждый раз проверяй в worktree, не верь diagnostics-блоку.

2. **Детерминизм генерации (templ + Tailwind) — ловушка №1.**
   - templ: `go generate ./...` (из каталога пакета) и `go tool templ generate` (из корня) дают
     РАЗНЫЙ `FileName` в `*_templ.go` → грязное дерево после gate. КАНОН: генерация из каталога
     пакета (Makefile `templ:` делает `cd internal/web && go tool templ generate`). Проверяй:
     после `make gate` и `make gen-check` дерево ОБЯЗАНО быть чистым (`git status` пусто).
   - Tailwind purge (`@source "../"`) сканирует utility-классы в .templ; набор классов меняется
     между правками → `app.css` ДРЕЙФУЕТ. Закоммиченный `static/css/app.css` ДОЛЖЕН быть в ФИНАЛЕ
     (после `make web-gen`), иначе gate его меняет. Это дважды кусало (C0-web, C0-auth).
   - `make gen-check` = `web-gen && git diff --exit-code` — твой детектор. Гоняй ПЕРЕД ACCEPT.

3. **Пустые коммиты `--allow-empty` (C0-config) — НЕ допускать.** Первый исполнитель схлопнул TDD-фазы
   в один коммит + пустышки. ТРЕБУЙ в промпте исполнителя: пофазные коммиты с РЕАЛЬНЫМ diff,
   red→green. Приёмщик/ревью это ловят. На C0-db/web/auth уже соблюдено — держи планку.

4. **gofmt НЕ входит в `go build/vet/test`.** Дважды файлы были не-gofmt-clean и прошли gate
   (C0-config login_test, C0-auth login_test+main.go). ВСЕГДА проверяй `gofmt -l internal/ cmd/`
   отдельно перед ACCEPT. Фикс — оркестратор сам `gofmt -w` (механика).

5. **go.mod indirect/tidy.** Новые прямые зависимости иногда помечаются `// indirect` или go.sum
   рассинхронен (C0-db). Проверяй `go mod tidy` (пустой diff) сам перед мержем.

6. **Артефакты/бинари не коммитить.** `/bin/` (Tailwind CLI 120МБ, gitignored), `/server` (бинарь
   cmd/server, gitignored). Проверяй `git ls-files | grep -E 'bin/|^server'` = пусто.
   КОММИТИТЬ нужно: `*_templ.go`, `static/css/app.css`, `static/vendor/htmx.min.js` (для чистой
   сборки без node/templ-бинаря/интернета).

7. **Сабагент-исполнитель иногда выходит с интерактивным вопросом «merge/PR?»** — ИГНОРИРУЙ его
   (мержит оркестратор по политике). В промпте исполнителя пиши «НЕ интерактивные вопросы — доделай и отчитайся».

8. **Тесты без сети/Postgres в дефолте.** Интеграционные (БД) — за `//go:build integration`
   (testcontainers, Docker ЕСТЬ в окружении, реально поднимается ~5-25с). OAuth — httptest, не
   реальный Google. Дефолтный `go test ./...` ОБЯЗАН быть зелёным без Docker/сети. GATE-команда
   БД: `make migrate-test` или `go test -tags=integration ./internal/db/...`.

9. **SendMessage недоступен** как инструмент — продолжить того же сабагента по agentId нельзя
   в новом чате; для LOOP запускай нового исполнителя с исчерпывающим контекстом дефекта.

10. **gen-check в worktree Codex ВСЕГДА exit 2 — это НЕ дефект.** Песочница Codex держит `.git`
    read-only → Codex не стейджит → `git diff --exit-code -- internal/web/` видит свои же
    НЕЗАКОММИЧЕННЫЕ правки и падает. ПРОВЕРЯЙ детерминизм САМ хешами: `md5sum app.css` ДО и ПОСЛЕ
    повторного `make web-gen` — если совпали, генерация детерминирована. gen-check exit 0 даёт
    смысл только на ЧИСТОМ дереве (после коммита). Так и было на reader-ui/reader-wiring.

11. **app.css ДРЕЙФУЕТ между worktree/моментами (грабля №2 в острой форме, словил на reader).**
    Закоммиченный в merge C `app.css` оказался устаревшим снимком (на 21 байт больше детерминированного
    выхода) — `gen-check` после merge показал 0 по стечению, но `make web-gen` давал ДРУГОЙ файл.
    Детектор: после merge атома с UI прогони `make web-gen` в ОСНОВНОМ дереве и проверь `git status` —
    если app.css изменился, закоммить детерминированный результат отдельным `fix(web)` (сделано: `dd2fc11`).
    Истинное значение = то, что СТАБИЛЬНО выдаёт `make tailwind` (проверь 2-3 прогона).

12. **Снос worktree: `bin` становится РЕАЛЬНЫМ каталогом, не симлинком.** Симлинк `bin` (на корневой
    `/bin` с tailwind) после `make web-gen`/`tailwind-bin` (`mkdir -p ./bin`) превращается в каталог.
    `rm -f .worktrees/<atom>/bin` тогда даёт «Is a directory» и ПРЕРЫВАЕТ цепочку `&&` → worktree не
    сносится. ПРАВИЛЬНО: вызывай `git worktree remove .worktrees/<atom> --force` НАПРЯМУЮ (он снесёт
    bin сам), без `rm -f bin &&` перед ним. Симлинк-вариант чистится `find -maxdepth 1 -name bin -type l -delete`.

13. **prod docker-compose требует `.env`.** `docker compose -f docker-compose.prod.yml config` падает
    «env file .env not found» без `.env` (там `env_file: .env`). Это НЕ дефект — prod требует секреты;
    README говорит «заполнить .env → make prod». Для проверки СИНТАКСИСА prod-compose заведи временный `.env`.

---

## КЛЮЧЕВЫЕ ФАКТЫ ПРОЕКТА (чтобы не перечитывать всё)
- Module: `github.com/LectureLog/lecturelog-web`, Go 1.25. Стек: chi+templ+htmx+Tailwind v4 (CSS-first), pgx/v5, tern (миграции).
- Слои `internal/`: coreclient (B1, клиент ядра + верификатор вебхука), config (env+fail-fast секрета),
  db (схема+миграции+CRUD users/sessions), web (каркас «Читальный зал»), auth (OAuth+сессии+CSRF+middleware).
- `cmd/server/main.go` — точка входа (config→db→auth→web→Serve).
- Дизайн-пакет в `design/` (tokens.css, STYLE_GUIDE.md, prototypes/*.dc.html) — вход для C1.
  Прототипы: Конспект→C1-reader, Хаб→C1-hub, Загрузка→C1-upload. Прототипы РАСХОДЯТСЯ по деталям
  (Общее.dc.html=1240px каркас, Конспект.dc.html=1180px читалка) — это норма, разные экраны.
- HMAC только на ВЕБХУКЕ ядро→платформа (платформа верифицирует); исходящие НЕ подписываются (долг доков B1 закрыт).
- Env: C0-config валидирует обязательные (GOOGLE_CLIENT_ID/SECRET, PLATFORM_CALLBACK_URL,
  LECTURELOG_WEBHOOK_SECRET, PLATFORM_DB_DSN, CORE_API_BASE_URL без /api/v1, CORE_MINIO_*).
  Опц: PRESIGNED_TTL=24h, SESSION_TTL=720h. Для cmd/server: PLATFORM_SECURE="true" в проде.

## ВОЛНА C1 (следующая после хвоста C0) — TASKS.md §C1
5 vertical-slices ∥: upload (§5), lecture (§3/§8), hub (§8), reader (§6), sync (§7).
GATE C1 = сквозной e2e. Долги, заложенные для C1: см. HANDOFF-C0.md (валидация upload до ядра,
индексы hub/owner в lectures, FileServer листинг, tabular-nums, канонизация email, чистка сессий).
Параллелизм C1 возможен (независимые модули), но взвесь токены/merge-конфликты — config/db/web/auth
теперь общие, доменные модули их потребляют.
